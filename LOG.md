# 二维热传导模拟项目开发日志

## 项目简介

本项目用于学习和实践 **Linux + C++ + 数值计算 + 科学可视化**。

通过实现一个简单的二维热传导模拟，逐步掌握：

* Linux 终端环境
* C++ 数值计算
* CMake / Ninja 构建系统
* Git 版本管理
* Python 数据处理与科学可视化
* 数值模拟与动态可视化
* 后续进一步探索 OpenGL / Vulkan / GPU 加速等方向

---

# 第一阶段：开发环境搭建

## 1.1 Linux 环境

使用 WSL 搭建 Ubuntu Linux 开发环境。

主要工具：

* Ubuntu 26.04 LTS
* g++
* CMake
* Ninja
* GDB
* Git
* Python
* Neovim
* ripgrep
* tree

项目目录：

```
~/project/Heat-Simulation
```

---

# 第二阶段：基础数值模拟

## 2.1 建立二维计算网格

首先建立二维空间网格，用于表示一块二维平面。

基本参数：

* 平面尺寸：1m × 1m
* x 方向网格数量：51
* y 方向网格数量：51
* 网格间距：0.02m

使用一维 `std::vector<double>` 存储二维温度场。

二维坐标：

```
(i, j)
```

转换为一维数组下标：

```
index = i + j * nx
```

这样可以在 C++ 中使用连续的一维内存表示二维数据。

---

## 2.2 初始化温度场

初始情况下，整个区域温度设置为：

```
20°C
```

然后将中心位置设置为高温：

```
100°C
```

边界保持固定温度：

```
20°C
```

因此模拟的基本过程是：

**中心高温 → 热量向周围扩散 → 温度逐渐趋于均匀**

---

## 2.3 实现二维热传导

二维热传导方程：

```
∂T/∂t = α(∂²T/∂x² + ∂²T/∂y²)
```

使用显式有限差分方法进行离散。

离散后的更新公式：

```
T(i,j)^(n+1)
=
T(i,j)^n
+
r[
    T(i+1,j)^n
    + T(i-1,j)^n
    + T(i,j+1)^n
    + T(i,j-1)^n
    - 4T(i,j)^n
]
```

其中：

```
r = αΔt / Δx²
```

当前参数：

* 热扩散系数 α = 0.01 m²/s
* 时间步长 Δt = 0.005 s
* 空间步长 Δx = 0.02 m
* r = 0.125

显式差分方法需要满足稳定性条件：

```
r ≤ 1/4
```

当前：

```
0.125 ≤ 0.25
```

因此满足稳定性要求。

---

## 2.4 使用双温度数组

模拟过程中使用两个温度数组：

* `temperature`
* `new_temperature`

当前时刻的数据只用于计算下一时刻的数据。

完成一次时间步后：

```
swap(temperature, new_temperature)
```

这样可以保证同一个时间步内所有计算都基于同一时刻的数据，避免新旧数据混合。

---

# 第三阶段：模拟数据导出

## 3.1 导出温度场

将 C++ 模拟得到的温度数据写入：

```
data/temperature.csv
```

CSV 数据包含：

```
step,time,x,y,temperature
```

其中：

* `step`：时间步
* `time`：模拟时间
* `x`：x 坐标
* `y`：y 坐标
* `temperature`：当前位置温度

这样可以将数值计算和后续可视化分离。

---

## 3.2 保存完整时间序列

模拟不再只保存最终结果，而是保存整个模拟过程。

当前设置：

* 时间步数：100
* 时间步长：0.005s
* 总模拟时间：0.5s
* 每个时间步保存完整的 51 × 51 温度场

因此 CSV 文件中包含：

```
101 个时间帧
×
51 × 51 个网格点
```

这样后续可以根据完整的时间序列恢复整个热扩散过程。

---

# 第四阶段：动态可视化

## 4.1 建立 Python 可视化流程

使用 Python 对 C++ 输出的数据进行处理。

主要使用：

* NumPy
* Matplotlib
* Pillow

基本流程：

```
C++ 数值模拟
      ↓
temperature.csv
      ↓
NumPy 读取数据
      ↓
Matplotlib 处理温度场
      ↓
动态动画
      ↓
temperature.gif
```

---

## 4.2 实现动态热传导动画

使用 Matplotlib 的 `FuncAnimation` 对同一个图像对象不断更新温度数据。

每一帧对应 C++ 模拟中的一个时间步。

温度场使用二维图像显示：

* 横轴：x 坐标
* 纵轴：y 坐标
* 图像亮度 / 颜色：温度
* 标题：当前模拟时间

最终生成：

```
data/temperature.gif
```

整个动画不是提前生成大量 PNG 图片，而是在 Python 中不断更新同一个可视化对象，然后将动画编码为 GIF。

---

# 第五阶段：后续可视化与性能优化方向

## 5.1 数据与动画优化

当前 Python 程序采用：

```
CSV → NumPy → Matplotlib → GIF
```

后续可以进一步优化数据读取和帧数据组织方式，减少动画过程中重复的数据筛选操作。

同时可以根据需要增加：

* 更长的模拟时间
* 更多时间步
* 更高的空间分辨率
* 更流畅的动画
* MP4 视频输出

---

## 5.2 科学可视化

当前主要使用二维温度场进行展示。

后续可以进一步加入：

* 等温线
* 温度曲面
* 温度变化曲线
* 不同位置的温度随时间变化
* 最大温度 / 平均温度变化
* 热量扩散范围
* 不同参数下的结果对比

逐步从简单的“看动画”发展到对模拟结果进行分析。

---

## 5.3 实时图形渲染方向

当前使用 Python + Matplotlib 进行科学可视化。

如果进一步向计算机图形学方向发展，可以尝试：

```
C++ 数值模拟
      ↓
OpenGL / Vulkan
      ↓
GPU
      ↓
实时渲染
```

此时可视化不再只是生成 GIF，而是实时显示模拟过程。

可以进一步研究：

* OpenGL
* Vulkan
* GLSL Shader
* GPU 计算
* CUDA
* 实时纹理更新
* GPU 渲染

---

## 5.4 性能优化方向

当前模拟规模较小，主要用于学习数值计算流程。

后续可以逐步扩大计算规模，并研究：

* OpenMP 并行计算
* SIMD
* CUDA
* GPU 加速
* 更大的二维网格
* 三维热传导
* 更复杂的边界条件
* 更高阶数值方法
* 更高效的数据结构

同时可以逐渐将项目拆分为：

```
数值计算模块
      ↓
数据输出模块
      ↓
可视化模块
      ↓
实时渲染模块
```

最终形成一个更加完整的数值模拟程序。

---

# 当前项目整体结构

```
Heat-Simulation/
├── src/
│   └── main.cpp
├── scripts/
│   └── animate.py
├── data/
│   ├── temperature.csv
│   └── temperature.gif
├── build/
│   ├── build.ninja
│   ├── CMakeCache.txt
│   └── heat_sim
├── .venv/
├── CMakeLists.txt
├── .gitignore
└── LOG.md
```

---

# 当前技术链路

```
Linux / WSL
      ↓
   C++
      ↓
数值计算
      ↓
二维热传导模拟
      ↓
CSV 数据
      ↓
  Python
      ↓
NumPy 数据处理
      ↓
Matplotlib 可视化
      ↓
动态 GIF
```

---

# 项目阶段总结

目前已经完成：

* Linux 开发环境搭建
* C++ 基础程序
* CMake 构建系统
* Ninja 编译流程
* 二维计算网格
* 温度场初始化
* 二维热传导有限差分
* 时间步迭代
* 模拟数据保存
* 完整时间序列保存
* Python 数据读取
* Matplotlib 动态可视化
* GIF 动画生成

项目已经从单纯的 C++ 数值程序，逐步形成：

**数值模型 → C++ 模拟 → 数据输出 → Python 分析 → 动态可视化**

后续重点将逐渐从“完成模拟”转向：

**提高模拟规模 → 优化计算性能 → 深入数值方法 → 探索实时渲染 → 逐步接触 GPU 计算与图形学。**

